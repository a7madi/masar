import 'package:flutter/material.dart';

class TripOwnerForm extends StatelessWidget {
  const TripOwnerForm({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('صفحة معلومات منسق الرحلة')),
    );
  }
}